import { makeNewsBlocks } from "./newsFunctions";
import { generateMockData } from "../../tests/utils";

const inputData = generateMockData(2);

describe('makeNewsBlocks', () => {
    test("makeNewsBlocks returns correct result", () => {

    });
});